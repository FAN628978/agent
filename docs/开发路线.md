# 🤖 Claude Agent 开发路线图

> 从零基础到生产级 Agent 系统的完整学习路径

---

## 📍 总览

```
Phase 1: 基础构建  →  Phase 2: 能力扩展  →  Phase 3: 多智能体  →  Phase 4: 生产部署
  (2~4 周)               (3~5 周)               (4~6 周)               (持续迭代)
```

---

## Phase 1｜基础构建（2~4 周）

> **目标**：掌握与 Claude 通信的核心方式，能独立完成单轮/多轮对话 Agent

### 1.1 环境准备

- [ ] 注册 Anthropic 账号，获取 API Key
- [ ] 安装 SDK：`pip install anthropic` 或 `npm install @anthropic-ai/sdk`
- [ ] 阅读官方文档：[docs.claude.com](https://docs.claude.com)

### 1.2 Messages API 基础

```python
import anthropic

client = anthropic.Anthropic()

response = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    messages=[{"role": "user", "content": "你好，介绍一下自己"}]
)
print(response.content[0].text)
```

**核心概念**：
- `model`：模型选择（Opus / Sonnet / Haiku）
- `messages`：对话历史数组（role: user / assistant）
- `system`：系统提示词，定义 Agent 身份和行为
- `max_tokens`：输出长度限制

### 1.3 多轮对话管理

- [ ] 手动维护 `messages` 历史数组
- [ ] 实现对话记忆（追加每轮 user/assistant 消息）
- [ ] 理解 Token 消耗与上下文窗口（最大 200k / 1M token）

### 1.4 模型选择策略

| 模型 | 适用场景 | 特点 |
|------|----------|------|
| Claude Opus 4.6 | 复杂推理、深度分析 | 最强智能，成本较高 |
| Claude Sonnet 4.6 | 生产环境主力 | 智能与速度均衡 |
| Claude Haiku 4.5 | 高并发、实时响应 | 极速，成本最低 |

**✅ Phase 1 里程碑**：完成一个能多轮对话的命令行 Chatbot

---

## Phase 2｜能力扩展（3~5 周）

> **目标**：为 Agent 装备工具调用、外部集成和技能模块

### 2.1 Tool Use（工具调用）

这是 Agent 区别于普通 LLM 的核心能力——让模型能调用真实函数。

```python
tools = [{
    "name": "get_weather",
    "description": "获取指定城市的天气信息",
    "input_schema": {
        "type": "object",
        "properties": {
            "city": {"type": "string", "description": "城市名称"}
        },
        "required": ["city"]
    }
}]

response = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    tools=tools,
    messages=[{"role": "user", "content": "台北今天天气如何？"}]
)
```

**工具调用流程**：
```
用户输入 → Claude 决策调用工具 → 执行真实函数 → 结果返回 Claude → 最终回答
```

**练习任务**：
- [ ] 实现一个搜索工具（接入 Serper / Tavily API）
- [ ] 实现一个计算器工具
- [ ] 实现文件读写工具

### 2.2 MCP（Model Context Protocol）

MCP 是连接 AI 与外部系统的标准化协议，相当于 Agent 的"插件系统"。

```
Claude Agent
    ├── MCP Server: Google Drive（读取文档）
    ├── MCP Server: GitHub（管理代码仓库）
    ├── MCP Server: Slack（发送消息）
    └── MCP Server: 自定义业务系统
```

**学习路径**：
- [ ] 理解 MCP Client / Server 架构
- [ ] 使用现有 MCP Server（官方已有 50+ 集成）
- [ ] 开发自定义 MCP Server 接入内部系统

### 2.3 Agent Skills（技能模块）

Skills 是打包好的领域知识，让 Agent 具备专业能力。

```
skills/
├── pdf-processing/
│   ├── SKILL.md        ← 技能描述与指令
│   └── process.py      ← 可执行脚本
├── data-analysis/
│   ├── SKILL.md
│   └── analyze.py
```

**SKILL.md 基本结构**：
```yaml
---
name: pdf-processing
description: 当用户需要处理 PDF 文件时使用此技能
---

# PDF 处理技能

## 使用场景
- 提取文本内容
- 合并/拆分文件
...
```

### 2.4 Prompt Engineering 进阶

- [ ] 系统提示词设计（角色定义、行为约束、输出格式）
- [ ] Few-shot 示例注入
- [ ] Chain-of-Thought 推理引导
- [ ] XML 标签结构化输出

**✅ Phase 2 里程碑**：完成一个带工具调用的领域专家 Agent（如：能搜索+总结的研究助手）

---

## Phase 3｜多智能体系统（4~6 周）

> **目标**：构建多 Agent 协作的复杂任务处理系统

### 3.1 Claude Agent SDK

```bash
pip install claude-agent-sdk
```

```python
from claude_agent_sdk import query, ClaudeAgentOptions

options = ClaudeAgentOptions(
    system_prompt="你是一个专业的代码审查助手",
    max_turns=10,
    allowed_tools=["Read", "Write", "Bash"]
)

async for message in query(prompt="审查这个项目的安全漏洞", options=options):
    print(message)
```

### 3.2 多 Agent 架构模式

#### 模式一：顺序管道（Pipeline）
```
输入 → Agent A（数据收集）→ Agent B（分析处理）→ Agent C（报告生成）→ 输出
```

#### 模式二：主从编排（Orchestrator-Worker）
```
              ┌── Worker Agent（搜索）
Orchestrator──┤── Worker Agent（计算）
              └── Worker Agent（写作）
```

#### 模式三：并行处理
```
                 ┌── Sub-Agent（处理文档1）──┐
主 Agent ────────┤── Sub-Agent（处理文档2）──┼──→ 汇总 Agent → 结果
                 └── Sub-Agent（处理文档3）──┘
```

### 3.3 Human-in-the-Loop（人机协同）

| 自主级别 | 说明 | 适用场景 |
|----------|------|----------|
| 每步确认 | Agent 每次操作前请求批准 | 高风险、敏感操作 |
| 关键节点确认 | 仅在重要决策时介入 | 大多数生产场景 |
| 全自动 | 内置分类器判断风险 | 低风险批处理任务 |

### 3.4 状态与记忆管理

```
短期记忆  →  当前 Session 的 messages 历史
长期记忆  →  外部存储（向量数据库 / KV Store）
工作记忆  →  Files API 持久化文件
```

**推荐技术栈**：
- 向量存储：Pinecone / Weaviate / pgvector
- KV 存储：Redis / DynamoDB
- 文件持久化：Anthropic Files API

### 3.5 Managed Agents（托管 Agent）

- [ ] 使用 Claude Platform 的全托管 Agent Session
- [ ] 配置 Webhook 监听 Session 生命周期事件
- [ ] 实现 Multiagent Sessions（多 Agent 协作会话）

**✅ Phase 3 里程碑**：构建一个完整的多 Agent 系统（如：自动化研究报告生成系统）

---

## Phase 4｜生产部署（持续迭代）

> **目标**：将 Agent 系统推向生产，关注可靠性、安全性和成本

### 4.1 性能优化

- [ ] **Prompt Caching**：缓存重复的系统提示，降低 90% Token 成本
- [ ] **Message Batches API**：批量处理，提升吞吐量
- [ ] **流式输出（Streaming）**：改善用户体验
- [ ] **模型降级策略**：复杂任务用 Opus，简单任务自动切换 Haiku

### 4.2 可靠性建设

```python
import anthropic
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=4, max=10))
def call_claude_with_retry(messages):
    return client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=1024,
        messages=messages
    )
```

- [ ] 错误重试机制（指数退避）
- [ ] 超时控制与熔断器
- [ ] 降级回退策略

### 4.3 安全与合规

- [ ] **权限最小化**：只授予 Agent 必要的工具权限
- [ ] **输入验证**：防止 Prompt Injection 攻击
- [ ] **输出过滤**：敏感信息脱敏
- [ ] **审计日志**：记录所有 Agent 操作

### 4.4 监控与可观测性

**关键指标**：
- Token 消耗量与成本趋势
- 任务成功率 / 失败率
- 平均响应时间（P50 / P95 / P99）
- 工具调用频率与成功率

**推荐工具**：
- Langfuse / LangSmith（LLM 专用可观测性）
- Datadog / Grafana（通用监控）
- Sentry（错误追踪）

### 4.5 成本管理

| 优化手段 | 预估节省 |
|----------|----------|
| Prompt Caching | 缓存命中可节省 ~90% |
| 选择合适模型层级 | Haiku vs Opus 成本差 ~50x |
| Batches API | 享受折扣价格 |
| 输出长度控制 | 精确设置 max_tokens |

---

## 🗂️ 推荐项目练习

| 阶段 | 项目 | 技术点 |
|------|------|--------|
| Phase 1 | 多轮对话 CLI Chatbot | Messages API、历史管理 |
| Phase 2 | AI 研究助手 | Tool Use、Web Search、总结输出 |
| Phase 2 | 文档处理 Agent | Skills、Files API、PDF/Excel |
| Phase 3 | 自动化报告系统 | 多 Agent、并行处理、状态管理 |
| Phase 3 | 代码审查 Bot | Agent SDK、GitHub MCP、CI 集成 |
| Phase 4 | 生产级客服 Agent | 完整架构、监控、安全、成本优化 |

---

## 📚 核心学习资源

| 资源 | 链接 |
|------|------|
| 官方文档 | https://docs.claude.com |
| Agent SDK | https://github.com/anthropics/claude-agent-sdk-python |
| MCP 协议 | https://modelcontextprotocol.io |
| Prompt 工程指南 | https://docs.claude.com/en/docs/build-with-claude/prompt-engineering/overview |
| Cookbook 示例 | https://github.com/anthropics/anthropic-cookbook |
| 发布日志 | https://platform.claude.com/docs/en/release-notes/overview |

---

## ⚡ 快速参考：技术选型决策树

```
需要构建 Agent？
│
├── 单任务、无工具调用
│   └── → 直接使用 Messages API
│
├── 需要调用外部工具/API
│   └── → Messages API + Tool Use
│
├── 需要集成第三方系统（GitHub/Slack/Drive）
│   └── → MCP Server 集成
│
├── 需要领域专业知识
│   └── → Agent Skills
│
├── 需要多步骤自主任务
│   └── → Claude Agent SDK
│
└── 需要多 Agent 协作 + 生产托管
    └── → Claude Managed Agents + Multiagent Sessions
```

---

*最后更新：2026年5月 · 基于 Anthropic 官方文档整理*
